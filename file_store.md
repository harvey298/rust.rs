/profiles/
    ->  [PROFILE NAME (steam id)]/
        ->  config.toml
        ->  creds.json
        ->  persistent_ids.txt
        ->  servers/
            ->  [SERVER NAME (server id)]/
                -> pair.json 


